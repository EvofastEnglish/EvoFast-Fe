# EVO-Fast-Web

npm install react-i18next i18next --save

# create new NEXTAUTH_SECRET

node -e "console.log(require('crypto').randomBytes(32).toString('base64'))
"use client";

# custom style taiwind

https://tailwindcss.com/docs/theme
const handleInjectFakeToken = async () => {
await update({
user: {
access*token: "THANH_TRONG_TOKENB*)TESSTTSSN",
loginDate: new Date().toISOString(),
expiresIn: 5,
},
});
};

const handlegetNewsession=async ()=>{
const newSession = await getSession();
console.log(newSession);
}

const testlogin = async ()=>{
await authService.login("thanhtrong0209@gmail.com","Password123@");

}
